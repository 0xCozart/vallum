{
  "name": "iota-gaskit",
  "version": "0.0.0-prerelease",
  "private": true,
  "description": "Open-source toolkit for deploying, securing, monitoring, and integrating IOTA gas-sponsored transactions.",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build -w @iota-gaskit/shared-types && npm run build -w @iota-gaskit/policy-gateway && npm run build -w @iota-gaskit/sdk && npm run build -w @iota-gaskit/policy-gateway-service",
    "test": "npm run build && node --import tsx --test scripts/*.test.ts examples/**/*.test.ts packages/*/src/*.test.ts apps/*/src/*.test.ts",
    "typecheck": "npm run build && tsc --noEmit",
    "smoke:local": "npm run build && tsx scripts/smoke-local-gateway.ts",
    "smoke:demo-dapp": "npm run build && tsx scripts/smoke-demo-dapp-local.ts",
    "smoke:demo-browser": "npm run build && tsx scripts/smoke-demo-dapp-browser.ts",
    "readiness:testnet": "npm run build && tsx scripts/check-testnet-readiness.ts",
    "diagnose:gas-station": "npm run build && tsx scripts/diagnose-gas-station-upstream.ts",
    "execute:testnet-demo": "npm run build && tsx scripts/execute-testnet-sponsored-demo.ts",
    "readiness:testnet:example": "npm run build && tsx scripts/check-testnet-readiness.ts --env-file .env.example --expect-placeholders",
    "pack:check": "npm run build && npm pack --dry-run -w @iota-gaskit/shared-types -w @iota-gaskit/policy-gateway -w @iota-gaskit/sdk",
    "grant:check": "npm test && npm run typecheck && npm run smoke:local && npm run smoke:demo-dapp && npm run smoke:demo-browser && npm run readiness:testnet:example && npm run pack:check"
  },
  "devDependencies": {
    "@iota/iota-sdk": "^1.13.0",
    "@types/node": "^20.17.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  },
  "engines": {
    "node": ">=20"
  }
}
